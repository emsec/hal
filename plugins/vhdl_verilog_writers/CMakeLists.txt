option(PL_VHDL_VERILOG_WRITERS "PL_VHDL_VERILOG_WRITERS" ON)
if(PL_VHDL_VERILOG_WRITERS OR BUILD_ALL_PLUGINS)
    file(GLOB_RECURSE VHDL_VERILOG_WRITERS_INC ${CMAKE_CURRENT_SOURCE_DIR}/include/*.h)
    file(GLOB_RECURSE VHDL_VERILOG_WRITERS_SRC ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)

    hal_add_plugin(vhdl_verilog_writers
                   SHARED
                   HEADER ${VHDL_VERILOG_WRITERS_INC}
                   SOURCES ${VHDL_VERILOG_WRITERS_SRC}
                   )
endif()
