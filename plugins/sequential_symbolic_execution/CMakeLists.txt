option(PL_SEQUENTIAL_SYMBOLIC_EXECUTION "PL_SEQUENTIAL_SYMBOLIC_EXECUTION" OFF)
if(PL_SEQUENTIAL_SYMBOLIC_EXECUTION OR BUILD_ALL_PLUGINS)
    file(GLOB_RECURSE SEQUENTIAL_SYMBOLIC_EXECUTION_INC ${CMAKE_CURRENT_SOURCE_DIR}/include/*.h)
    file(GLOB_RECURSE SEQUENTIAL_SYMBOLIC_EXECUTION_SRC ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)
    file(GLOB_RECURSE SEQUENTIAL_SYMBOLIC_EXECUTION_PYTHON_SRC ${CMAKE_CURRENT_SOURCE_DIR}/python/*.cpp)

    hal_add_plugin(sequential_symbolic_execution
                   SHARED
                   HEADER ${SEQUENTIAL_SYMBOLIC_EXECUTION_INC}
                   SOURCES ${SEQUENTIAL_SYMBOLIC_EXECUTION_SRC} ${SEQUENTIAL_SYMBOLIC_EXECUTION_PYTHON_SRC}
                   LINK_LIBRARIES PUBLIC ${Z3_LIBRARIES} z3_utils
                   )
endif()
