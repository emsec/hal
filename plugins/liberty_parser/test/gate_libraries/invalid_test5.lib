library (TEST_GATE_LIBRARY) {
    define(cell);
    cell(TEST_LUT) {
        lut ("lut_out") {
            data_category     : "test_category";
            data_identifier   : "test_identifier";
            bit_order         : "north-east";
        }
        pin(I) {
            direction: input;
        }
        pin(O) {
            direction: output;
            function: "lut_out";
        }
    }
}