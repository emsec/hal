library (TEST_GATE_LIBRARY) {
    define(cell);
    cell(TEST_GATE_TYPE) {
        pin(I) {
            direction: input;
        }
        pin(O) {
            direction: output;
            function: "!I";
        }
    }
    cell(TEST_GATE_TYPE) {
        pin(I0) {
            direction: input;
        }
        pin(O0) {
            direction: output;
            function: "!I0";
        }
    }
}