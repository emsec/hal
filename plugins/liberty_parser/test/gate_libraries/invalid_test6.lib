library (TEST_GATE_LIBRARY) {
    define(cell);
    cell(TEST_GATE_TYPE) {
        pin(I) {
            direction: WAMBO;
        }
        pin(O) {
            direction: output;
            function: "I";
        }
    }
}