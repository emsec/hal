library (TEST_GATE_LIBRARY) {
    define(cell);
    cell(TEST_GATE_TYPE) {
        pin(I) {
            direction: input;
        }
        pin(I_nodir) {
            capacitance : 1.0;
        }
        pin(O) {
            direction: output;
            function: "I";
        }
    }
}