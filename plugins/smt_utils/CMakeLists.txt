option(PL_SMT_UTILS "PL_SMT_UTILS" OFF)

if(PL_SMT_UTILS OR BUILD_ALL_PLUGINS)
    file(GLOB_RECURSE PL_SMT_UTILS_INC ${CMAKE_CURRENT_SOURCE_DIR}/include/*.h)
    file(GLOB_RECURSE PL_SMT_UTILS_SRC ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)
    file(GLOB_RECURSE PL_SMT_UTILS_PYTHON_SRC ${CMAKE_CURRENT_SOURCE_DIR}/python/*.cpp)

    hal_add_plugin(smt_utils
        SHARED
        HEADER ${PL_SMT_UTILS_INC}
        SOURCES ${PL_SMT_UTILS_SRC} ${PL_SMT_UTILS_PYTHON_SRC}
        PYDOC SPHINX_DOC_INDEX_FILE ${CMAKE_CURRENT_SOURCE_DIR}/documentation/smt_utils.rst
    )
endif()
